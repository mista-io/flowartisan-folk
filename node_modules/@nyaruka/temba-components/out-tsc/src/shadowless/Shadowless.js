import { __decorate } from "tslib";
import { customElement, property } from 'lit/decorators';
import { LitElement } from 'lit';
let Shadowless = class Shadowless extends LitElement {
    createRenderRoot() {
        for (const child of this.childNodes) {
            if (child.className == 'content') {
                return child;
            }
        }
        const root = document.createElement('div');
        root.className = 'content';
        this.appendChild(root);
        return root;
    }
    createRenderRoots() {
        const root = document.createElement('div');
        root.className = 'content';
        this.appendChild(root);
        return root;
    }
    render() {
        return this.body;
    }
};
__decorate([
    property({ type: Object })
], Shadowless.prototype, "body", void 0);
Shadowless = __decorate([
    customElement('temba-shadowless')
], Shadowless);
export default Shadowless;
//# sourceMappingURL=Shadowless.js.map