import { fixture, assert } from '@open-wc/testing';
import { ContactChat } from '../src/contacts/ContactChat';
import { getHTML } from '../test/utils.test';
import './utils.test';
const getChatHTML = (attrs = {}) => getHTML('temba-contact-chat', attrs);
describe('temba-contact-chat', () => {
    it('can be created', async () => {
        const chat = await fixture(getChatHTML());
        assert.instanceOf(chat, ContactChat);
    });
});
//# sourceMappingURL=temba-contact-chat.test.js.map